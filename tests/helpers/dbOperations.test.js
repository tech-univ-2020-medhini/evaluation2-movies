const {get} = require('../../src/helpers/dbOperations');
const db  = require('../../models/index');
describe('The db operations', () => {
	describe('The get function',() => {
		it('', async() => {
			// const mockDb = jest.spyOn();
			// mockDb.mockResolvedValue();
			await get();
			// mockDb.mockRestore();
		});
	});
});
